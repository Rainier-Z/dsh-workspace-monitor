- insert:
    - id: dsh-workspace-monitor
      name: dsh-workspace-monitor
      config:
        intervalMs: 60000
        ignore:
          - .git
          - node_modules
          - .dsh-workspace-monitor
        maxEntries: 100000
        maxChanges: 200
